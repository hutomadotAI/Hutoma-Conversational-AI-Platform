<source>
  @type forward
  port 24224
</source>

<match API-applog-v1.**>
  @type stdout
  log_level error
</match>

<match API-access-v1.**>
  @type stdout
  log_level error
</match>

<match API-servicesStatus-v1.**>
  @type stdout
  log_level error
</match>

<match API-chatlog-v1.**>
  @type stdout
  log_level error
</match>